apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mvp-sample-front
  annotations:
    #ingress.kubernetes.io/rewrite-target: /$1
    kubernetes.io/ssl-redirect: "false"
spec:
  #tls:
  #  - secretName: cloudb-secret
  rules:
    - host: hklee.front.169.56.84.35.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix 
            backend:
              service:
                name: mvp-sample-front
                port:
                  number: 8081